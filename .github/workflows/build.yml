name: CI

on:
  push:
    branches: [ $default-branch ]
    tags:
      - "*"
      - "!**-alpha**"
      - "!**-beta**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: BigWigsMods/packager@v2
